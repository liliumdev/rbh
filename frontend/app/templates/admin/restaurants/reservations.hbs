{{title model.restaurant.name " - Reservations"}}

{{admin-nav-bar addShown=false plusShown=false}}

   <div class="row">
      <div class="col-md-12 col-sm-12 col-xs-12 col-centered">
          <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12 page-title v-align-center">
				        Reservations at {{ model.restaurant.name }}
            </div>
          </div>
          <div class="row">
              <div class="col-md-12">

                        {{full-calendar events=events defaultView='agendaWeek' header=headerOptions eventClick=(action 'eventClicked')}}

                  <div class="card">
                      <div class="content">
                 <!--     	<table>
                      		<tr>
	                      		<th>Name</th>
                            	<th>Reservation made</th>
	                      		<th>Number of guests</th>
	                      		<th>Date</th>
	                      		<th>Time</th>
							</tr>
              {{#if (eq model.reservations.length 0)}}
                <tr>
                  <td colspan="6">
                    There are no reservations for this restaurant.
                  </td>
                </tr>
              {{/if}}
							 {{#each model.reservations as |reservation|}}   
								<tr>
		                      		<td>{{reservation.account.firstName}} {{reservation.account.lastName}}</td>
                              <td>{{moment-format reservation.createdAt 'MMM DD, YYYY'}}</td>
		                      		<td>{{reservation.persons}}</td>
		                      		<td>{{moment-format reservation.forTime 'MMM DD, YYYY'}}</td>
		                      		<td>{{moment-format reservation.forTime 'h:mm A'}}</td>
								</tr> 
							{{/each}}
                      	</table>-->
                      </div>
                  </div>
              </div>
          </div>
       </div>
       
   </div>
   
<div class="modal fade" id="reservationModal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-body">
        <button type="button" class="close" data-dismiss="modal"><span class="fa fa-remove" aria-hidden="true"></span></button>
        
        <p class="title">Reservation</p>

        <p>

          <br>
          <table border="0">
          <tr>
            <td>For:</td> 
            <td>{{event.name}}</td>
          </tr>
          <tr>
            <td>Persons:</td> 
            <td>{{event.persons}}</td>
          </tr>

          </table>
          <br><br>
          {{#if event.request}}
            {{event.request}}
          {{else}}
            There is no special request for this reservation.
          {{/if}}
          <br>
        </p>
      </div>
    </div>
  </div>
</div>